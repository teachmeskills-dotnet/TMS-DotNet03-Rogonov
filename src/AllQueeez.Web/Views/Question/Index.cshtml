@model IEnumerable<QuestionsViewModel>

@{
    ViewData["Title"] = "Questions";
}

<div class="wrapper">
    <div class="header">
        <div class="header-menu">
            <div class="title">All<span>Queez</span></div>
            <div class="sidebar-btn">
                <i class="fas fa-bars"></i>
            </div>
            <div class="logout-btn">
                <a asp-controller="Account" asp-action="Logout"><i class="fas fa-sign-out-alt"></i></a>
            </div>
        </div>
    </div>
    <div class="sidebar">
        <div class="sidebar-menu">
            <div class="profile">
                <img src="~/images/1.jpg" alt="">
                <p>Tkumwa</p>
            </div>
            <ul>
                <li class="item">
                    <a href="game" class="menu-btn">
                        <i class="fas fa-columns"></i><span>My queezes</span>
                    </a>
                </li>
                <li class="item">
                    <a href="theme" class="menu-btn">
                        <i class="fas fa-star"></i><span>Themes</span>
                    </a>
                </li>
                <li class="active-item">
                    <a href="question" class="menu-btn">
                        <i class="fas fa-question"></i><span>Questions</span>
                    </a>
                </li>
                <li class="item">
                    <a href="#" class="menu-btn">
                        <i class="fas fa-cog"></i><span>Settings</span>
                    </a>
                </li>
            </ul>
        </div>
    </div>
    <div class="main-container">
        <div class="title-row">
            <span class="title">Questions</span>
            <a href="question/create" class="create-btn">Add question</a>
        </div>
        <div class="search">
            <i class="fas fa-search">
                <input type="text"
                       placeholder="Type in question name"
                       onfocus="this.placeholder = ''"
                       onblur="this.placeholder = 'Type in question name'">
            </i>
        </div>
        <table class="table-header">
            <thead>
                <tr>
                    <th width="10%">TITLE</th>
                    <th width="35%">DESCRIPTION</th>
                    <th width="30%">COMMENT</th>
                    <th width="15%">QUESTION TYPE</th>
                    <th width="10%">ACTIONS</th>
                </tr>
            </thead>
        </table>
        @foreach (var question in Model)
        {
            <table class="table-row">
                <tbody>
                    <tr>
                        <td width="10%">@question.Title</td>
                        <td width="35%">@question.Description</td>
                        <td width="30%">@question.Comment</td>
                        <td width="15%">TYPE</td>
                        <td width="10%">
                            <a href=""><i class="fas fa-pen"></i></a>
                            <a asp-controller="Question" asp-action="Delete" asp-route-id="@question.Id"><i class="fas fa-trash"></i></a>
                        </td>
                    </tr>
                </tbody>
            </table>
        }
    </div>
</div>

@section css {
    <link href="~/css/tables.css" rel="stylesheet" />
}